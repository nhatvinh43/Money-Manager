<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/transaction_item">
    <TextView
        android:id="@+id/title_chooseCurrency"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/chooseUnit"
        android:textColor="@color/white"
        android:fontFamily="sans-serif-medium"
        android:textAlignment="center"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:textSize="22sp"/>
    <androidx.appcompat.widget.SearchView
        android:id="@+id/searchBar_chooseCurrency"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="@drawable/authforms"
        android:layout_below="@id/title_chooseCurrency"
        android:layout_marginHorizontal="30dp"
        android:layout_marginTop="20dp"
        android:theme="@style/AppTheme"
        app:queryHint="@string/searchUnit"/>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginHorizontal="30dp"
        android:layout_below="@id/searchBar_chooseCurrency"
        android:layout_marginTop="20dp">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/unitList_chooseCurrency"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="30dp"
            android:paddingBottom="15dp" />

    </androidx.core.widget.NestedScrollView>


</RelativeLayout>